{% extends 'base.html.twig' %}

{% block title %}Przelicznik Walut{% endblock %}

{% block body %}

    {% include 'includes/headerUser.html.twig' %}
    <div class="container d-flex justify-content-center mt-4">
        <h3 class="mx-2">Wymiana walut: </h3>
        <a href="{{ path('exchangeXtoPLN') }}" class="btn btn-exchange mx-2">X -> PLN</a>
        <a href="{{path('exchangePLNtoX')}}" class="btn btn-exchange mx-2">PLN -> X</a>
    </div>
    {% include 'includes/currenciesTable.html.twig' %}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}