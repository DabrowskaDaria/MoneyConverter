{% extends 'base.html.twig' %}
{% block body %}
    <div class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="{{ path('homePageForUser') }}"><b>Aplikacja przeliczania walut</b></a>
            <div class="ms-auto d-flex align-items-center gap-2">
                <i class="fa-regular fa-circle-user fa-xl"></i>
                <h3 class="mb-0">Witaj!</h3>
            </div>
            <div class="dropdown">
                <button class="btn btn-light dropdown-toggle d-flex align-items-center gap-2" type="button" id="userDropdown" data-bs-toggle="dropdown" aria-expanded="false"></button>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                    <li><a class="dropdown-item" href="{{path('userActivity')}}">Aktywność</a></li>
                    <li><a class="dropdown-item" href="{{path('app_logout')}}">Wyloguj się</a></li>
                </ul>
            </div>
        </div>

    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}